# Supabase — Rede de Guias

Execute as migrations em ordem no SQL Editor do projeto já ligado ao Bento Pantanal.

Arquivo atual:

- `migrations/20260901090000_rede_guias.sql`

O arquivo pode ser executado uma vez sobre a estrutura antiga. Ele preserva `guides`, normaliza os status antigos e recria as políticas RLS do módulo de guias para não manter políticas permissivas legadas.
